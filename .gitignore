# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# The shared design-system build output IS tracked: both Vercel projects consume it
# without a build step (apps/web via a file: dep), so it must be committed.
!packages/ui/dist
!packages/ui/dist/**

# Environment files
.env
.env.*
!.env.example

# Test output
coverage
playwright-report
test-results
cucumber-reports

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
bun.lockb
.vercel

# Google API credentials
scripts/gsc-credentials.json
scripts/gsc-token.json

# Supabase CLI local cache (machine-specific, regenerated on link)
supabase/.temp/
