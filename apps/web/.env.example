# Next.js uses the NEXT_PUBLIC_ prefix (not VITE_). Same Supabase project/keys as
# the existing app. Copy to .env.local for local dev; set as env vars in the
# Next app's Vercel project for deploys.
NEXT_PUBLIC_SUPABASE_URL=https://wqaplkypnetifpqrungv.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
