{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "cd ../.. && npm ci --include=dev && npm run build --workspace @altogether/ui && cd apps/web && next build"
}
